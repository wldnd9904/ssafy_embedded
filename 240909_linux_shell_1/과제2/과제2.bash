# 1. home directory 이동 후, 전체 디렉토리 확인
cd ~
ls -al
# 2. 현재 디렉토리가 어디인지 확인한다.
pwd
# 3. root directory 이동 후, 전체 디렉토리 확인
cd /
ls -al
# 4. 화면을 모두 지운다.
clear
# 5. /usr/bin 으로 이동한다.
cd /usr/bin
# 6. 화면을 모두 지운다.
clear
# 7. 이전 디렉토리로 이동한다.
cd -
# 8. 현재 디렉토리가 어디인지 출력한다.
pwd
# 9. 터미널을 종료한다. (exit 명령어)
exit